# {{ title }}

> 来源：B站视频 · UP主：{{ owner.name }}
> 原始链接：{{ url }}
{% if pubdate %}> 发布时间：{{ pubdate }}（北京时间）
{% endif %}> 视频时长：{{ duration_min }}分{{ duration_sec }}秒

---

## 一、视频概览

| 项目 | 内容 |
|------|------|
| **标题** | {{ title }} |
| **UP主** | {{ owner.name }} |
| **BV号** | {{ bvid }} |
{% if tags %}| **标签** | {{ tags | join('、') }} |
{% endif %}{% if stat.view is defined %}| **播放量** | {{ stat.view }} |
| **点赞** | {{ stat.like }} |
| **投币** | {{ stat.coin }} |
| **收藏** | {{ stat.favorite }} |
| **转发** | {{ stat.share }} |
| **评论** | {{ stat.reply }} |
{% endif %}

{% if desc %}
### 视频简介

{{ desc }}

{% endif %}
{% if subtitle.available and subtitle.full_text %}
## 二、视频字幕（完整转录）

{% if subtitle.lan %}> 语言：{{ subtitle.lan }}
{% endif %}
{{ subtitle.full_text }}

{% else %}
## 二、精华内容

> ⚠️ {{ subtitle.note or '该视频未提供字幕' }}

> 该视频 UP 主未上传字幕，视频简介为空。精华内容需由 AI 基于视频标题、标签及评论区讨论推断还原，
> 或通过 `--ima-raw-md` 参数提供外部生成的完整精华 Markdown 文档上传。

{% endif %}
{% if top_replies %}
## 三、高赞评论

{% for r in top_replies %}
### {{ loop.index }}. {{ r.like }} 赞 · {{ r.rcount }} 回复 — {{ r.uname }}

> {{ r.content }}

{% if r.top_sub_reply %}> └─ **[{{ r.top_sub_reply.like }} 赞] {{ r.top_sub_reply.uname }}**：{{ r.top_sub_reply.content }}
{% endif %}
{% endfor %}
{% endif %}
## 四、参考资料

- 原始链接：{{ url }}

---

> ⚠️ **重要声明**
>
> 本文档由 url-extract v{{ version }}（学术测试版）生成。当视频无字幕时，文档中的部分内容系从社区中相同或相似标题的公开资料处获取并整合而成，**并非直接由视频本身的音频或画面提取**。部分要点可能来自社区作者的二次解读，而非视频创作者的原话。视频中独有的演示画面、语气强调、即兴发挥等内容无法被覆盖。文档的结构化呈现可能掩盖内容可信度的局限性。
>
> **如需用于正式参考、引用或决策，请务必对照原始视频核实。**
> 本工具请勿用于生产环境。
